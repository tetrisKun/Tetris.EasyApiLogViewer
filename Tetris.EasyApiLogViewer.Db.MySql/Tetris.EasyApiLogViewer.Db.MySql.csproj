<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <AssemblyName>Tetris.EasyApiLogViewer.Db.MySql</AssemblyName>
        <RootNamespace>Tetris.EasyApiLogViewer.Db.MySql</RootNamespace>

        <!-- NuGet 包信息 -->
        <PackageId>Tetris.EasyApiLogViewer.Db.MySql</PackageId>
        <Version>0.9.0</Version>
        <Title>Easy API Log Viewer - MySQL Provider</Title>
        <Description>MySQL 数据库提供程序，用于 Tetris.EasyApiLogViewer 的数据持久化。</Description>
        <Authors>Tetris</Authors>
        <Copyright>Copyright (c) 2025 Tetris</Copyright>
        <PackageTags>aspnetcore;logging;api;mysql;database</PackageTags>
        <PackageProjectUrl>https://github.com/tetrisKun/Tetris.EasyApiLogViewer</PackageProjectUrl>
        <RepositoryUrl>https://github.com/tetrisKun/Tetris.EasyApiLogViewer</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="MySql.Data" Version="9.2.0" />
      <PackageReference Include="Tetris.EasyApiLogViewer.Db.Abstract" Version="1.0.0" />
    </ItemGroup>

</Project>
